# 七大奇迹对决 · 使用说明

一个可以玩的《七大奇迹对决》（7 Wonders Duel）电子版。支持**本地热座**、**人机对战**、**联机对战**。

压缩包里已经带了构建好的成品，**解压后不需要 `npm install`，也不需要联网下载游戏文件**。

---

## 一、最省事的开始方式

### 需要什么

- 一台装了 **Node.js 18 或更高版本** 的电脑（[nodejs.org](https://nodejs.org) 下载 LTS 版即可）
- 没了

检查一下 Node 是否装好：打开命令行输入

```
node -v
```

能显示 `v18.x.x` 或更高就可以。

### 启动

解压后直接双击：

```text
启动游戏.exe
```

启动程序会自动选择可用端口、运行服务器并打开浏览器。启动窗口需要保持开启；关闭窗口即停止游戏。

如果启动程序被安全软件拦截，也可以双击备用脚本：

```bat
启动服务器.bat
```

或者手动执行：

```
node dist-server\server.mjs
```

看到类似这样的输出就成功了：

```
七大奇迹对决 · 联机服务器已启动
--------------------------------------------------
本机访问：    http://localhost:8080
局域网访问：  http://192.168.5.4:8080   <- 同一 WiFi 下的另一台设备用这个
--------------------------------------------------
```

使用备用脚本时，在浏览器里打开 **http://localhost:8080** 即可游玩。

### 停止

回到启动窗口，按 `Ctrl + C` 或直接关闭窗口。

---

## 二、四种玩法

打开页面会先停在**首页模式选择屏**，不会自动开局。四张卡片对应四种玩法：

```
🤖 人机对战　　👥 本地热座　　🌐 联机对战　　🎭 单人 Solo
```

- **点一张卡片**即选中该模式（默认选中「人机对战」），卡片内会就地展开它的参数：
  人机 → 难度三档；单人 Solo → 对手领袖（可选「随机」，共 5–9 位，随扩充变化）；
  本地热座 → 一句说明；**联机对战 → 点卡片直接进房间大厅**（大厅有自己的开始流程）。
- 卡片下方的**开局设置**里有 🏛 万神殿 / 🏛 市政广场 两个扩充开关，可任意组合；
  「当前：…」一行会实时汇总你选的东西。
- 都设好后点 **开始对局** 才真正开局。
- 进入对局后，顶栏仍然可以切模式 / 难度 / 扩充（切扩充会按新设置重开一局）。

### 1. 人机对战（默认）

你是**玩家一**，AI 执**玩家二**。在「人机对战」卡片里选难度：

| 难度 | 特点 | 出招速度 |
|---|---|---|
| 简单 | 只看眼前一步，偶尔失误 | 几乎瞬间 |
| 中等 | 会考虑你的回应，会拦科技/军事 | 约 0.5 秒 |
| 困难 | 会推演多层、会对暗牌做概率估计 | 1～3 秒 |

### 2. 本地热座

点「本地热座」卡片 → 开始对局，两个人轮流用同一台设备。适合面对面玩。

### 3. 联机对战

点「联机对战」卡片会直接进入房间大厅。

- 一台设备点 **创建房间**，会得到一个 6 位房间码（比如 `3CQ6YH`）
- 另一台设备输入这个房间码，点 **加入**
- 两边都点 **我准备好了**
- 房主点 **开始对局**

**手机 / 平板怎么加入**：让电脑当服务器，手机连同一个 WiFi，然后手机浏览器打开电脑屏幕上打印的那个「局域网访问」地址（形如 `http://192.168.5.4:8080`）。手机上不需要安装任何东西。

房间内点「再来一局」会**自动交换先后手**。

### 4. 单人 Solo

点「单人 Solo」卡片 → 在卡片里选对手领袖（或留「随机」）→ 开始对局，一个人对抗官方 Solo
变体的领袖 AI。「开局设置」里的 **🏛 万神殿** 与 **🏛 市政广场** 开关同样适用于 Solo
（对应官方 Solo 扩展变体），开启后领袖池会相应增加。
Solo 同样支持「复盘本局」：领袖回合按当时同一条随机流重演，复盘里还能看到每一步领袖抽到的决策卡。

---

## 三、怎么操作

1. **点一张高亮的牌**（发光的、没被压住的牌）
2. 底部出现三个选择：
   - **建造建筑** — 付钱把牌放进你的城市。按钮上会显示要花多少金币
   - **弃牌换 N 金币** — 拿钱走人。N = 2 + 你城里黄卡数量
   - **建造奇迹** — 用这张牌当材料，盖你的一座奇迹
3. 选完，轮到对手

> **键盘玩法**：方向键在牌阵里移动（会有橙色焦点框），`Enter` 选定并行动、
> 连按再执行主操作（能建则建、否则弃牌），`W` 打开/收起建造奇迹、`1~9` 直接选第几座奇迹、
> `Esc` 取消。全部弹窗里的按钮都可以用 `Tab` + `Enter` 操作。

### 对局结束还能复盘

- 结算画面点「**复盘本局**」进入回放：拖动进度条可跳到任意一步，支持单步、自动播放与倍速；
  联机对局双方也都能复盘。
- 复盘是纯只读的，画面由「开局种子 + 动作日志」逐帧重放得到，和当时完全一致。

### 几个容易忽略的点

- **资源不会消耗**。造了伐木场，木材就一直有，可以反复用来造东西。
- **缺的资源可以买**。价格 = 2 金 + 对手棕/灰卡上该资源的符号数。对手产得越多，你买得越贵。
- **牌阵里的暗牌对双方都看不见**。你拿走一张牌，可能会翻开下面的牌——有时是帮了对手。
- **连锁免费**：卡面左下角有连锁符号的话，持有对应的前置卡就能免费造。鼠标悬停（手机上长按）能看到说明。

### 三种获胜方式

| 方式 | 条件 | 时机 |
|---|---|---|
| 军事压制 | 冲突标记推进到对方首都 | 立即结束 |
| 科技压制 | 集齐 6 种不同科技符号 | 立即结束 |
| 终局计分 | 时代 III 结束后比总分 | 打完全场 |

平局时比蓝色（市政）建筑的分数。

---

## 四、界面速查

- **顶部军事轨道**：左边是你推进的格数，右边是对手。进入「兵临城下」（3 格）对手掉 2 金，「深入腹地」（6 格）掉 5 金。
- **左右两侧面板**：双方的城市（按颜色分组）、奇迹、发展标记、金币。
- **中间牌阵**：当前时代的牌。面板标题会显示「已缩放至 xx%」，手机上点 **放大查看** 可以看原始尺寸并左右滑动。
- **右下日志**：最近发生的事，最新的在最上面。
- **顶栏 🔊/🔇**：音效开关，默认关闭。

---

## 五、常见问题

**Q：打不开页面 / 提示端口被占用？**
优先使用 `启动游戏.exe`，它会在 8080～8099 中自动选择可用端口。使用备用脚本时，
可执行 `set PORT=9000 && node dist-server\server.mjs`，然后访问 `http://localhost:9000`。

**Q：手机连不上电脑的服务器？**
- 确认手机和电脑在**同一个 WiFi**（有些路由器的「访客网络」会隔离设备）
- 确认电脑防火墙允许 Node.js 通行，或临时关掉防火墙试试
- 确认用的是「局域网访问」那个地址，不是 `localhost`

**Q：关掉命令行后游戏还在吗？**
不在了。`node` 进程就是服务器，关掉即停。

**Q：音效默认怎么是关的？**
怕突然出声吓到人。顶栏点一下 🔇 就能开，设置会记住。

**Q：我想改代码 / 自己构建？**
需要联网装一次依赖：

```
npm install
npm run dev        # 开发模式，改代码即时生效
npm run build      # 构建前端
npm run server:build   # 重新打包服务器
```

改完源码后要重新执行 `npm run build` 和 `npm run server:build`，再启动服务器才会生效。

**Q：断线了怎么办？**
刷新页面（房间码记在浏览器里）能凭断线前的身份回到原来的座位继续。
超过 90 秒仍未回来，**不会判你输**——你的座位会由 AI 临时托管继续打完，你随时回来都能接管回去。

---

## 六、压缩包里都有什么

```
启动游戏.exe          Windows 一键启动，自动打开浏览器
启动服务器.bat       备用启动方式
使用说明.md           本文件
版本信息.txt           构建日期与运行要求
dist-server/          打包好的服务器
dist/                 构建好的前端
```

压缩包不含源码、开发工具、缓存和 `node_modules`。电脑仍需预先安装 Node.js 18 或更高版本。

---

## 七、规则范围

实现的是游戏**基础版**全部规则：三时代轮抽、资源与贸易、连锁免费、战争旗与区块罚金、
7 种科技符号与 10 枚发展标记、12 座奇迹与全场 7 座上限、三种胜利判定与终局计分。

**已实现扩充**（顶栏 🏛 开关，可自由组合，切换后立即开始新对局）：

- **万神殿 Pantheon**：5 组神格牌堆与图板、时代 I 神话 token 放置神格、
  时代 II/III 图标调用神明、2 座新奇迹（圣所 / 通神大剧场）；
- **市政广场 Agora**：参议院与密谋、16 条法令、政治家 / 阴谋家行动、
  Curia Julia / Knossos 奇迹的新能力。

另外实现了**官方 Solo 变体**（含上述两个扩展的 Solo 规则与对应领袖）。

**没有**实现：官方变体规则（如"给对手选牌"等）。

---

## 八、扩展怎么开

顶栏点 **🏛 万神殿关 / 🏛 市政广场关** 即可切换，显示"开"即生效，
切换会**立即开始新对局**。奇迹选择阶段弹窗盖住顶栏时，先完成草稿再操作开关。
